{
  "operation": "List Embedding Models",
  "method": "GET",
  "path": "/v1/models/embedding",
  "auth": "Bearer token (LETTA_API_KEY)",
  "request": {
    "method": "GET",
    "path": "/v1/models/embedding",
    "headers": {
      "Authorization": "Bearer $LETTA_API_KEY"
    }
  },
  "response": {
    "status": 200,
    "body": [
      {
        "embedding_model": "text-embedding-3-small",
        "embedding_dim": 1536
      }
    ]
  }
}